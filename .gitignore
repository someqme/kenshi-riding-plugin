# Editor / AI tooling
.claude/
.continue/
.vscode/

# Internal dev notes (not for the public repo)
CLAUDE.md
doc.md
RidingPlugin_HISTORY.md
TASK.md
TEST_REQUIRED.md

# Backups, logs, personal helper scripts
*.bak
*.bak_*
*.log
dmp.py

# Superseded seat-table snapshots: kept on disk for provenance, not worth publishing.
# snapshots/ itself IS tracked - it holds the inputs the shipped kDefaultSeats[] was
# generated from (see doc.md, 内置默认座位).
snapshots/_superseded/

# Build intermediates (should never be inside the tree, but just in case)
*.obj
*.pdb
*.ilk
*.exp
__pycache__/
*.pyc

# OS cruft
Thumbs.db
Desktop.ini
